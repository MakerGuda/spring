spring:
  application:
    name: spring
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        import-check:
          enabled: false
        enabled: true
        group: DEFAULT_GROUP
        name: spring
        refresh-enabled: true
        namespace:
